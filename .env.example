POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_USER=hub_user
POSTGRES_PASSWORD=hub_password
POSTGRES_DB=financial_hub

POSTGRES_READONLY_USER=hub_readonly
POSTGRES_READONLY_PASSWORD=hub_password

# 下载根目录（可选）。由 financial_hub 自动注入为 DOWNLOAD_ROOT/<source_type>。
# 独立运行时可手动设置；留空则使用项目内 subtitles 目录。
# CRAWLER_OUTPUT_DIR=/data/financial_hub/downloads/youtube
